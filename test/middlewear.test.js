const chai = require("chai");
const expect = chai.expect;
const data = require("./jsonPayload");


describe("Web Hook MiddleWear",()=>{
	// drop elasticsearch index
	// create index if does exists
	it("Should add data in elasticsearch",(done)=>{
		done();
	})
})